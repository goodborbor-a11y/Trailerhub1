name: CI and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: trailerhub-production
  cancel-in-progress: false

jobs:
  test:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Build frontend
        run: |
          npm ci
          npx tsc --noEmit
          npm run build
      - name: Build backend and run billing and SEO tests
        run: |
          npm ci --prefix server
          npm --prefix server run build
          npm --prefix server run test:billing
          npm --prefix server run test:sharp
          npm --prefix server run test:seo

  deploy:
    name: Deploy to production
    needs: test
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Run restricted production deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: /usr/local/sbin/trailerhub-deploy
